# Minimal config for Zeabur
host: ""
port: 8317

auth-dir: "/root/.cli-proxy-api"

api-keys:
  - "xiaoju-cpa-key"

debug: false

remote-management:
  allow-remote: true
  secret-key: "xiaoju-admin"
  disable-control-panel: false

request-retry: 3
